const Cover = () => {
  return (
        <div className="overlay cover h-[30%] w-full bg-cover bg-center bg-gradient-to-r to-gradient-secondary from-gradient-primary flex items-center justify-center">
        </div>
  );
};

export default Cover;
